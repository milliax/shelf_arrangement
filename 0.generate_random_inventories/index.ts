import { prisma } from "./lib/prisma";


(() => {
    // This script generates random inventories and saves them to the database.
    // It is intended to be run in a development environment only.



    
})()